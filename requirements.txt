# By default, this file installs the CPU-only version of PyTorch to keep the production
# VPS deployment lightweight. If you have a CUDA-capable GPU for local development,
# install the GPU version instead of the CPU version by running:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://download.pytorch.org/whl/cpu

flask==3.1.3
flask-sqlalchemy==3.1.1
flask-limiter==4.1.1
opencv-contrib-python-headless==4.13.0.92
easyocr==1.7.2
torch==2.12.1+cpu
torchvision==0.27.1+cpu
pytest==9.1.0
pillow==12.1.1
gunicorn==26.0.0
onnxruntime==1.24.4
numpy==2.4.4
python-dotenv==1.0.1
